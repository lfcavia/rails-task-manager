<h1>This is index view</h1>
<ul>
  <% @tasks.each do |t| %>
   <li><input type="checkbox" value=<%= t.completed %> disabled><%= link_to t.title, task_path(t)%></li>
  <% end %>
</ul>

<p><%= link_to "Add new task", tasks_new_path %></p>
